ffw_joint_state_broadcaster:
  # Note: joint_groups is a nested parameter structure that cannot be fully defined
  # using generate_parameter_library. The actual joint_groups configuration should be
  # set in the controller configuration YAML file.
  # 
  # Example configuration in controller YAML:
  # joint_groups:
  #   arm_left:
  #     topic_name: "/leader/arm_left/joint_states"
  #     joints: ["arm_l_joint1", "arm_l_joint2", ...]
  #   arm_right:
  #     topic_name: "/leader/arm_right/joint_states"
  #     joints: ["arm_r_joint1", "arm_r_joint2", ...]
  #
  # The broadcaster will dynamically read these nested parameters at runtime.
  #
  # Dummy parameter to satisfy generate_parameter_library (not used in code)
  _dummy: {
    type: bool,
    default_value: false,
    description: "Dummy parameter to satisfy generate_parameter_library. Not used in code."
  }
